/** @type {import('next').NextConfig} */
const nextConfig = {
      images: {
    domains: ['res.cloudinary.com'], // Replace 'example.com' with the domain of your image
  },};

export default nextConfig;
